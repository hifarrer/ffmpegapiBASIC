# Docker/Railway (Python 3.13). replit-object-storage is omitted: no 3.13-compatible
# wheel on PyPI; Railway uses volume paths anyway. Replit installs deps from pyproject.toml (Python 3.11).
flask>=3.1.1
flask-sqlalchemy>=3.1.1
gunicorn>=23.0.0
psycopg2-binary>=2.9.10
werkzeug>=3.1.3
requests>=2.32.5
sqlalchemy>=2.0.43
openai>=1.0.0
