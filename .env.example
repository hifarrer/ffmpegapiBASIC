# Copy to .env and fill in (never commit .env)
SESSION_SECRET=
# HTTPS origin for absolute download/storage URLs (Replit production). No trailing slash.
PUBLIC_BASE_URL=https://www.ffmpegapi.net
DATABASE_URL=
# or NEON_DATABASE_URL=
OPENAI_API_KEY=
